# Stripe API Keys (Optional - will run in test mode if not provided)
# Get your test keys from: https://dashboard.stripe.com/test/apikeys
# For production, use live keys from: https://dashboard.stripe.com/apikeys

# Publishable key (starts with pk_test_ or pk_live_)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# Secret key (starts with sk_test_ or sk_live_)
STRIPE_SECRET_KEY=

# Note: The application will run in test mode if these are not set.
# Test mode allows checkout without real payments.